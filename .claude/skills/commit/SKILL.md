# Commit and Push
1. Run typecheck, lint, and tests first. Stop if any fail.
2. Stage all changes with `git add -A`
3. Write a concise conventional commit message based on the changes
4. Commit and push to the current branch
