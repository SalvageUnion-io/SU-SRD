[install]
# Use isolated linker for better dependency isolation and security
linker = 'isolated'

[test]
# Preload happydom for app tests - paths are relative to workspace root
preload = ["./test/happydom.ts", "./test/testing-library.ts"]
# Enable parallel test execution for faster test runs

