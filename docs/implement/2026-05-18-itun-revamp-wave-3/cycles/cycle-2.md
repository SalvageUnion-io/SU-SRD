# Cycle-2 — Mech Builder (AC-2, #190)

## Status: complete

## AC Covered

- **AC-2**: Mech builder at `apps/in-the-union-now/src/routes/mechs/new.tsx` provides chassis selector + systems/modules grid + cargo. `capacity.ts` wired so over-slot selection surfaces clear violations (red affordance + CapacityIndicator badges). Scrap math and cargo capacity computed and displayed. Auto stand-in pilot rendered on preview. Finishing calls `entityStore.create('mech', ...)`.

## Artifacts Written

### New files

- `apps/in-the-union-now/src/routes/mechs/new.tsx` — TanStack Router file route; wraps MechBuilder and navigates to `/` on success
- `apps/in-the-union-now/src/components/mech/MechBuilder.tsx` — top-level builder form orchestrating all sub-components
- `apps/in-the-union-now/src/components/mech/ChassisSelector.tsx` — native `<select>` of all chassis from `SalvageUnionReference.Chassis.all()`
- `apps/in-the-union-now/src/components/mech/SystemModuleGrid.tsx` — tabbed systems/modules picker; shows "Over cap" badge on items that would breach slot cap (no hard block, user sees violation and can undo)
- `apps/in-the-union-now/src/components/mech/CargoEditor.tsx` — add/remove custom cargo items; slot count per item
- `apps/in-the-union-now/src/components/mech/CapacityIndicator.tsx` — sticky sidebar panel with slot bars + violation badges (`data-testid` targets for tests)
- `apps/in-the-union-now/src/components/mech/__tests__/MechBuilder.test.tsx` — 10 tests covering the minimum bar (ChassisSelector, CapacityIndicator violations, CargoEditor, entityStore.create on submit)
- `apps/in-the-union-now/src/routeTree.gen.ts` — regenerated by `bunx vite build` to include `/mechs/new`
- `docs/implement/2026-05-18-itun-revamp-wave-3/cycles/cycle-2.md` (this file)

## Test Evidence

```
129 pass
0 fail
Ran 129 tests across 16 files
```

All 10 new tests pass. Pre-existing tests (capacity.ts, cargo.ts, entityStore, etc.) continue to pass.

## Typecheck / Lint Evidence

- `bun --filter in-the-union-now typecheck` — clean (0 errors)
- `bun --filter in-the-union-now lint` — clean (0 errors, 0 warnings from new files)
- Pre-existing failures in `suref-react` (ClassAbilityTreeDisplay, NestedChassisAbility) and `itun-legacy` are NOT from this cycle; confirmed present in bootstrap commit HEAD.

## Rules wired

- `computeMechCapacity` from `src/lib/rules/capacity.ts` called on every systems/modules change
- `computeCargoCapacity` from `src/lib/rules/cargo.ts` called on every cargo change
- `salvageValueFor` / `scrapCostFor` from `src/lib/rules/scrap.ts` are available for display but not yet surfaced in the builder UI (Wave 3 spec does not require showing scrap cost in the creation flow; deferred to sheet view in Wave 4)

## Deviation from plan

None. Cycle is file-disjoint from cycles 1/3/4 as designed. ConditionToggle is NOT imported (plan note: Wave 3 builder scope, ConditionToggle wiring is cycle-4 territory).

## Branch

`run/2026-05-18-itun-revamp-wave-3/cycle-2`
