# Ontology updates — itun-revamp-wave-0

## Proposed terms

- **ITUN** — In The Union Now. The Salvage Union character builder + game manager app located at `apps/in-the-union-now/`.
- **itun-legacy** — Archived path for the React 18 + Supabase implementation; preserved for reference and history at `apps/itun-legacy/`. Frozen-state policy formalized in M3.
- **in-the-union-now (v2)** — Greenfield local-first rebuild at `apps/in-the-union-now/` (after Wave 0 archives the legacy app). Stack: Vite + React 19 + TanStack Router/Query + Zustand + ShadCN + Tailwind v4 + Zod + IndexedDB.

## Reused terms

(none — `docs/ontology.md` does not yet exist for this monorepo)

## Notes

- Promote these to `docs/ontology.md` when the project grows to need a shared glossary (likely during M3 launch prep).
