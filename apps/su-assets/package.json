{
  "name": "su-assets",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker serving licensed entity artwork from the su-lp-assets R2 bucket, with -440/-880 derivatives rendered on demand through Cloudflare Images",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:coverage": "bun test --coverage --coverage-reporter=text --coverage-reporter=lcov --coverage-dir=coverage"
  },
  "dependencies": {
    "observability": "workspace:*"
  }
}
