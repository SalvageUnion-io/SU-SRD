/**
 * Salvage Union pilots have a standard max HP 10 and max AP 5 (core rules);
 * class data does not encode these, so they live here as named constants for
 * the pip trackers and for seeding fresh pilots at full HP/AP.
 */
export const PILOT_MAX_HP = 10
export const PILOT_MAX_AP = 5
