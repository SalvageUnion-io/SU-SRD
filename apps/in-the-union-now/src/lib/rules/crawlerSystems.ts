/**
 * Weapon-system detection for crawler capacity (pure-ish — reads the ORM).
 *
 * Moved to packages/salvageunion-reference/lib/rules/crawlerSystems.ts
 * (ADR-006). Thin re-export shim — see that module for the implementation.
 */

export { isWeaponSystem } from 'salvageunion-reference/rules'
