# Logs
logs
*.log
.legacy/

# Dependencies
node_modules
**/node_modules

# Build outputs
dist
dist-ssr
**/dist
build-ladle
*.local
*.tsbuildinfo
core
.design-bundle/

# Test/E2E output
**/playwright-report/
**/test-results/

# Vite
.vite
apps/suref-web/.vite

# TanStack Router temporary files
**/.tanstack

# Ladle
.ladle

# Supabase CLI cache
**/supabase/.temp/

# Environment variables
.env
.env.local
.env.*.local

# Claude Code
.claude/settings.local.json
.mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Netlify
.netlify

# Salvage Union rules content is NOT checked into git. The source PDFs and their
# text extract / authoring briefs (rules/) are copyright-bearing verbatim
# material; the condensed digest (docs/rules/) is derived from them. Both are
# generated and local-only — produce them with `bun run rules:regen`. Only the
# tooling (tools/rules-digest/, tools/extract-rules.ts) lives in git.
rules/*
docs/rules/
